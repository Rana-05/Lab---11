# Lab-11

This lab uses K-Means clustering to group credit card customers based on their behavior. The data was cleaned, scaled, and analyzed using different visualization methods.

Main tasks:

Data cleaning
Feature scaling
K-Means clustering
Elbow Method
PCA visualization
